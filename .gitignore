# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
.env.*.local

# Claude Code (local-only)
.claude/settings.local.json
.claude/launch.json
**/.claude/launch.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Raw uncompressed source assets (compressed versions are committed instead)
*_raw.mp4
*_raw.webm
*_raw.mov

# Reference material — brand guides, brochures, extracted page renders.
# All dev-only; never commit to repo. /docs is the local-only reference folder.
docs/
**/scratch/

# Internal session log — kept LOCAL only as a running engineering diary.
# Useful when picking up a session in a fresh Claude window, but should
# never be pushed to GitHub (private notes, large file, churns every day).
claudeconversation.txt

# Reference PDFs that the client uploaded into /public for designer reference.
# Per user instruction (5 May 2026): "isko hatana mat", but never push to git.
# These are huge (>100 MB) and would break Netlify deploy if shipped.
tac-preview/public/*.pdf
tac-preview/public/**/*.pdf
