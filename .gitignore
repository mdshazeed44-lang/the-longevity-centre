# Dependencies
node_modules/

# Build output
dist/
build/
.vite/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env
.env
.env.local
.env.*.local

# Claude Code (local-only)
.claude/settings.local.json
.claude/launch.json
**/.claude/launch.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# Raw uncompressed source assets (compressed versions are committed instead)
*_raw.mp4
*_raw.webm
*_raw.mov

# Reference material — brand guides, brochures, extracted page renders.
# All dev-only; never commit to repo. /docs is the local-only reference folder.
docs/
